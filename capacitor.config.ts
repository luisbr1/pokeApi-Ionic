import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.ronanzenatti.pokeapi',
  appName: 'pokeApi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
